# **shell**

echo os.system('/bin/bash')  
  
/bin/sh -i  
  
perl —e 'exec "/bin/sh";'  
  
perl: exec "/bin/sh";  
  
lua: os.execute('/bin/sh')  
  
IRB: exec "/bin/sh  
  
vi: :!bash  
  
:set shell=/bin/bash:shell  
  
vim ':!bash'